import { ShortenPathPipe } from './shorten-path.pipe';

describe('ShortenPathPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenPathPipe();
    expect(pipe).toBeTruthy();
  });
});
